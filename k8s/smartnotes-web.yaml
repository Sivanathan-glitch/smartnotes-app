apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartnotes-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: smartnotes-ui
  template:
    metadata:
      labels:
        app: smartnotes-ui
    spec:
      containers:
      - name: smartnotes-ui
        image: smartnotesacr.azurecr.io/smartnotes-ui:v2
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: smartnotes-ui-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: smartnotes-ui
